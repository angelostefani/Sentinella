TZ=Europe/Rome

POSTGRES_DB=assistant
POSTGRES_USER=assistant
POSTGRES_PASSWORD=assistant
DATABASE_URL=postgresql+psycopg://assistant:assistant@postgres:5432/assistant

JWT_SECRET=change-me-super-secret
JWT_EXPIRE_HOURS=12

# Password del primo utente admin (generata random se non impostata, scritta in /tmp/admin_initial_password.txt)
ADMIN_PASSWORD=

# Origini CORS consentite (virgola-separato). Lasciare * per uso locale.
# Per produzione: CORS_ORIGINS=https://yourdomain.com
CORS_ORIGINS=*

OLLAMA_URL=http://ollama:11434
OLLAMA_MODEL=llama3.2

SEARXNG_URL=http://searxng:8080

RATE_LIMIT_RPM=60

MAX_FETCH_BYTES=2000000
FETCH_TIMEOUT_S=15
MAX_TEXT_CHARS_PER_SOURCE=4000

WORKER_MAX_WORKERS=4
WATCHLIST_SYNC_INTERVAL_S=30
WATCH_JITTER_MAX_S=30

OLLAMA_TIMEOUT_S=600
